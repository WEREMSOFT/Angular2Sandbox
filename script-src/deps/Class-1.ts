export class CLASS {
    public m_number: number;
    public m_string: string;
    public m_array: string[];

// [PROPERTIES]
// [UNIONS]

    constructor(public data: string){
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
        this.m_array.push(Math.random().toString());
    }

    //[METHODS]

}